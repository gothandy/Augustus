@model IEnumerable<Account>

<div class="well-sm">
    <div class="list-group">
        @foreach (var a in Model)
            {
            <a href="@Url.Action("Invoices", "Account", new { id = a.Id })" class="list-group-item">@a.Name</a>
        }
    </div>
</div>
