@model Invoice

<ol class="breadcrumb">
    <li class="hidden-xs"><a href="@Url.Action("ActiveAccounts", "Organization")">True Clarity</a></li>

    <li class="hidden-xs"><a href="@Url.Action("Opportunities", "Account", new { id = Model.Account.Id })">@Model.Account.Name</a></li>

    <li>@Html.ActionLink(Model.Opportunity.Name, "Invoices", "Opportunity", new { id = Model.Opportunity.Id }, null)</li>
    <li>@Html.ActionLink(Model.Name, "Invoices", "Opportunity", new { id = Model.Id }, null)</li>
</ol>
