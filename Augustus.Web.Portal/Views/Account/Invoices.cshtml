@model Account
@{
    ViewBag.Title = Model.Name;
}

@Html.Partial("Breadcrumb")

<ul class="nav nav-tabs">
    <li role="presentation" class="active"><a href="@Url.Action("Invoices", new { id = Model.Id })">Invoices</a></li>
    <li role="presentation"><a href="@Url.Action("Opportunities", new { id = Model.Id })">Opportunities</a></li>
</ul>

<div class="well-sm">
    @if (Model.Invoices.Count() == 0)
    {
        <div class="well-sm">
            <div class="panel">No invoices</div>
        </div>
    }
    else
    {

        <div class="table-responsive">
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th>Date</th>
                        <th>Name</th>
                        <th class="currency">Margin</th>
                        <th class="currency">Revenue</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var i in Model.Invoices)
                {
                        <tr>
                            <td>@DateTimeHelper.Format(i.InvoiceDate)</td>
                            <td>@Html.ActionLink(i.Name, "Details", "Invoice", new { id = i.Id }, null)</td>
                            <td class="currency">@CurrencyHelper.Format(i.Margin)</td>
                            <td class="currency">@CurrencyHelper.Format(i.Revenue)</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

    }
</div>

<div>
    <a class="btn btn-default" href="@Url.Action("Create", "Invoice", new { id = Model.Id })">Create Invoice</a>
    <a class="btn btn-default pull-right" href="@Url.Action("Edit", "Account", new { id = Model.Id })">Edit Account</a>
</div>
